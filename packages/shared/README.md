# packages/shared

**Status:** scaffold

Shared TypeScript / JSON schema types for API contracts between `apps/web` and documentation consumers. Python services define Pydantic models locally in Phase 1.
