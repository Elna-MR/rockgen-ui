# apps/web

**Status:** Phase 1

Next.js Disease and Protein workspaces.

```bash
cd apps/web
npm install
npm run dev
```

Routes:
- `/` — RockGen home
- `/diseases` — disease list
- `/diseases/als` — ALS dashboard
- `/proteins/P07737` — PFN1 workspace
