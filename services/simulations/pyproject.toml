[project]
name = "rockgen-simulations"
version = "0.1.0"
description = "RockGen Protein Dynamics Engine"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["rockgen_simulations"]
